/**
 * contextlab CLI — watch, cost, why, optimize, doctor.
 *
 * Thin: parses argv, calls core and store, renders. Populated on day 2.
 *
 * @param {string[]} argv
 * @returns {void}
 */
export function run(argv) {
  void argv
  process.stdout.write('contextlab: not implemented yet\n')
}
