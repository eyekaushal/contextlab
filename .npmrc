# Workspace packages are linked, never fetched from the registry.
link-workspace-packages=true
prefer-workspace-packages=true

# pnpm's default isolated node_modules: a package can only import what it
# declares. The proxy's zero-dependency rule depends on this staying true.
strict-peer-dependencies=false
